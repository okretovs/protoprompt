# Security rules

- Never commit secrets or tokens.
- Treat environment variables as sensitive unless explicitly public.
- Validate user input at trust boundaries.
- Avoid logging credentials, auth headers, tokens, or private payloads.
- Keep dependencies minimal and maintained.
